{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/genomic-medicine-sweden/TRANA/nextflow_schema.json",
    "title": "genomic-medicine-sweden/TRANA pipeline parameters",
    "description": "A nextflow pipeline for the EMU tool from treangenlab.",
    "type": "object",
    "$defs": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define where the pipeline should find input data and save output data.",
            "required": ["outdir"],
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "exists": true,
                    "schema": "assets/schema_input.json",
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.csv$",
                    "description": "Path to comma-separated file containing information about the samples in the experiment.",
                    "help_text": "You will need to create a design file with information about the samples in your experiment before running the pipeline. Use this parameter to specify its location. It has to be a comma-separated file with 3 columns, and a header row.",
                    "fa_icon": "fas fa-file-csv"
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "The output directory where the results will be saved. You have to use absolute paths to storage on Cloud infrastructure.",
                    "fa_icon": "fas fa-folder-open"
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "description": "Less common options for the pipeline, typically set in a config file.",
            "help_text": "These options are common to all nf-core pipelines and allow you to customise some of the core preferences for how the pipeline runs.\n\nTypically these options would be set in a Nextflow config file loaded for all pipeline runs, such as `~/.nextflow/config`.",
            "properties": {
                "version": {
                    "type": "boolean",
                    "description": "Display version and exit.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "validate_params": {
                    "type": "boolean",
                    "description": "Boolean whether to validate parameters against the schema at runtime",
                    "default": true,
                    "fa_icon": "fas fa-check-square",
                    "hidden": true
                },
                "parameters_schema": {
                    "type": "string",
                    "default": "nextflow_schema.json",
                    "description": "JSON schema file used for parameter validation.",
                    "fa_icon": "fas fa-cog",
                    "hidden": true
                },
                "publish_dir_mode": {
                    "type": "string",
                    "default": "copy",
                    "description": "Method used to save pipeline results to output directory.",
                    "help_text": "The Nextflow `publishDir` option specifies which intermediate files should be saved to the output directory. This option tells the pipeline what method should be used to move these files. See [Nextflow docs](https://www.nextflow.io/docs/latest/process.html#publishdir) for details.",
                    "fa_icon": "fas fa-copy",
                    "enum": ["symlink", "rellink", "link", "copy", "copyNoFollow", "move"],
                    "hidden": true
                },
                "publish_dir_overwrite": {
                    "type": "boolean",
                    "hidden": true
                },
                "trace_timestamp": {
                    "type": "string",
                    "default": "2025-08-08_11-53-55",
                    "hidden": true
                },
                "email_on_fail": {
                    "type": "string",
                    "description": "Email address for completion summary, only when pipeline fails.",
                    "fa_icon": "fas fa-exclamation-triangle",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$",
                    "help_text": "An email address to send a summary email to when the pipeline is completed - ONLY sent if the pipeline does not exit successfully.",
                    "hidden": true
                },
                "plaintext_email": {
                    "type": "boolean",
                    "description": "Send plain-text email instead of HTML.",
                    "fa_icon": "fas fa-remove-format",
                    "hidden": true
                },
                "max_multiqc_email_size": {
                    "type": "string",
                    "description": "File size limit when attaching MultiQC reports to summary emails.",
                    "pattern": "^\\d+(\\.\\d+)?\\.?\\s*(K|M|G|T)?B$",
                    "default": "25.MB",
                    "fa_icon": "fas fa-file-upload",
                    "hidden": true
                },
                "multiqc_title": {
                    "type": "string",
                    "description": "MultiQC report title. Printed as page header, used for filename if not otherwise specified.",
                    "fa_icon": "fas fa-file-signature"
                },
                "monochrome_logs": {
                    "type": "boolean",
                    "description": "Do not use coloured log outputs.",
                    "fa_icon": "fas fa-palette",
                    "hidden": true
                },
                "hook_url": {
                    "type": "string",
                    "description": "Incoming hook URL for messaging service",
                    "fa_icon": "fas fa-people-group",
                    "help_text": "Incoming hook URL for messaging service. Currently, MS Teams and Slack are supported.",
                    "hidden": true
                },
                "multiqc_config": {
                    "type": "string",
                    "description": "Custom config file to supply to MultiQC.",
                    "fa_icon": "fas fa-cog",
                    "hidden": true
                },
                "multiqc_logo": {
                    "type": "string",
                    "description": "Custom logo file to supply to MultiQC. File name must also be set in the MultiQC config file",
                    "fa_icon": "fas fa-image",
                    "hidden": true
                },
                "multiqc_methods_description": {
                    "type": "string",
                    "description": "Custom MultiQC yaml file containing HTML including a methods description.",
                    "fa_icon": "fas fa-cog"
                },
                "schema_ignore_params": {
                    "type": "string",
                    "hidden": true
                },
                "tracedir": {
                    "type": "string",
                    "description": "Directory to keep pipeline Nextflow logs and reports.",
                    "default": "null/pipeline_info",
                    "fa_icon": "fas fa-cogs",
                    "hidden": true
                },
                "email": {
                    "type": "string",
                    "description": "Email address for completion summary.",
                    "fa_icon": "fas fa-envelope",
                    "help_text": "Set this parameter to your e-mail address to get a summary e-mail with details of the run sent to you when the workflow exits. If set in your user config file (`~/.nextflow/config`) then you don't need to specify this on the command line for every run.",
                    "pattern": "^([a-zA-Z0-9_\\-\\.]+)@([a-zA-Z0-9_\\-\\.]+)\\.([a-zA-Z]{2,5})$"
                },
                "show_hidden_params": {
                    "type": "boolean",
                    "fa_icon": "far fa-eye-slash",
                    "description": "Show all params when using `--help`",
                    "hidden": true,
                    "help_text": "By default, parameters set as _hidden_ in the schema are not shown on the command line when a user runs with `--help`. Specifying this option will tell the pipeline to show all parameters."
                }
            }
        },
        "emu": {
            "title": "EMU",
            "type": "object",
            "description": "Options for EMU",
            "default": "",
            "properties": {
                "db": {
                    "type": "string",
                    "description": "Real path to the database used by emu",
                    "default": "assets/databases/emu_database/"
                },
                "seqtype": {
                    "type": "string",
                    "default": "map-ont",
                    "description": "short-read: sr, Pac-Bio:map-pb, ONT:map-ont, High-Quality Long-Reads: lr:hq"
                },
                "min_abundance": {
                    "type": "number",
                    "default": 0.0001,
                    "description": "min species abundance in results"
                },
                "minimap_max_alignments": {
                    "type": "integer",
                    "default": 50,
                    "description": "miminimap max number of secondary alignments per read"
                },
                "minibatch_size": {
                    "type": "integer",
                    "default": 500000000,
                    "description": "minibatch size for minimap2 mapping"
                },
                "keep_read_assignments": {
                    "type": "boolean",
                    "default": true,
                    "description": "Output file with likelihoods."
                },
                "keep_files": {
                    "type": "boolean",
                    "description": "Keep sam file"
                },
                "output_unclassified": {
                    "type": "boolean",
                    "default": true,
                    "description": "Output fiels for unclassified reads: 1) File containing reads that was not mapped to the database, 2) File containing reads that mapped to the database but was presumed to be not in the sample by EMU\u00b4s algorithm."
                },
                "combine_levels": {
                    "type": "string",
                    "default": "family",
                    "description": "Taxonomic level which emu combine-output will make results for. Choose between species,genus,family,order,class,phylum,clade,superkingdom,subspecies,species subgroup,species group"
                },
                "combine_counts": {
                    "type": "boolean",
                    "description": "Estimated read counts instead of relative abundance",
                    "default": true
                },
                "combine_split_tables": {
                    "type": "boolean",
                    "description": "Two output tables:abundances and taxonomy lineages"
                }
            },
            "required": ["db"]
        },
        "short_read_options": {
            "title": "short read options",
            "type": "object",
            "description": "",
            "default": "",
            "properties": {
                "FW_primer": {
                    "type": "string",
                    "description": "Forward primer"
                },
                "RV_primer": {
                    "type": "string",
                    "description": "Reverse primer"
                },
                "skip_cutadapt": {
                    "type": "boolean",
                    "description": "Skip primer trimming with cutadapt"
                },
                "cutadapt_min_overlap": {
                    "type": "integer",
                    "default": 3,
                    "description": "Minimum overlap for valid matches of primer sequences with reads for cutadapt (-O)."
                },
                "cutadapt_max_error_rate": {
                    "type": "number",
                    "default": 0.1,
                    "description": "Sets the maximum error rate for valid matches of primer sequences with reads for cutadapt (-e)."
                },
                "retain_untrimmed": {
                    "type": "boolean",
                    "description": "Cutadapt will retain untrimmed reads, choose only if input reads are not expected to contain primer sequences."
                },
                "save_intermediates": {
                    "type": "boolean",
                    "description": "Save trimmed files from cutadapt"
                }
            }
        },
        "preprocessing_long_reads": {
            "title": "Preprocessing long reads",
            "type": "object",
            "description": "Options concering preprocessing of long read data",
            "default": "",
            "properties": {
                "merge_fastq_pass": {
                    "type": "string",
                    "description": "Path to a directory containing barcode directories. For nanopore data. This option merges the fastq-files in the barcode directories for each barcode folder. Use the flag in combination with \"--barcodes_samplesheet\" to get correct sample ids for each result file. If  merge_barcodes_samplesheet is not defined, the result files will have the prefix according to respective barcode derectory."
                },
                "barcodes_samplesheet": {
                    "type": "string",
                    "description": "Path to barcode samplesheet. Tab separated samplesheet where column1 hold the barcode directory e.g., barcode01, and column2 holds the sample id. Headers: barcode sample_id.  Flag is used in combination with \"merge_fastq_pass\".  With this flag , the results files are named after sample id and not barcode directory."
                },
                "quality_filtering": {
                    "type": "boolean",
                    "default": true,
                    "description": "Filter reads by quality and length"
                },
                "longread_qc_qualityfilter_minlength": {
                    "type": "integer",
                    "default": 1200,
                    "description": "Minimum length for reads"
                },
                "longread_qc_qualityfilter_maxlength": {
                    "type": "integer",
                    "default": 1800,
                    "description": "Maximum length for reads"
                },
                "longread_qc_qualityfilter_min_mean_q": {
                    "type": "integer",
                    "default": 94,
                    "description": "minimum mean quality threshold (identity)"
                },
                "save_preprocessed_reads": {
                    "type": "boolean",
                    "description": "save preprocessed reads"
                },
                "save_merged_reads": {
                    "type": "boolean",
                    "default": true,
                    "description": "save merged ont reads"
                },
                "save_downsampled_reads": {
                    "type": "boolean",
                    "default": false,
                    "description": "save downsampled reads"
                }
            }
        },
        "krona_options": {
            "title": "krona options",
            "type": "object",
            "description": "",
            "default": "",
            "properties": {
                "run_krona": {
                    "type": "boolean",
                    "default": true,
                    "description": "Run krona, true or false"
                },
                "krona_taxonomy_tab": {
                    "type": "string",
                    "default": "assets/databases/krona/taxonomy/taxonomy.tab",
                    "description": "Path to the taxonomy.tab used by Krona"
                }
            }
        },
        "porechop_abi_options": {
            "title": "porechop_abi options",
            "type": "object",
            "description": "Adapter trimming for long read data",
            "default": "",
            "properties": {
                "adapter_trimming": {
                    "type": "boolean",
                    "description": "Adapter trimming for long read data. Requires a list with adapters to trim"
                }
            }
        },
        "phyloseq_options": {
            "title": "Phyloseq options",
            "type": "object",
            "description": "",
            "default": "",
            "properties": {
                "phyloseq": {
                    "type": "boolean",
                    "description": "Create a phyloseq object. Works only if there are 2 samples or more in the run."
                }
            }
        },
        "seqtk_options": {
            "title": "seqtk options",
            "type": "object",
            "description": "",
            "default": "",
            "properties": {
                "downsample_n_reads": {
                    "type": "integer",
                    "description": "Subsample reads to this size"
                }
            }
        },
        "institutional_config_options": {
            "title": "Institutional config options",
            "type": "object",
            "fa_icon": "fas fa-university",
            "description": "Parameters used to describe centralised config profiles. These should not be edited.",
            "help_text": "The centralised nf-core configuration profiles use a handful of pipeline parameters to describe themselves. This information is then printed to the Nextflow log when you run a pipeline. You should not need to change these values when you run a pipeline.",
            "properties": {
                "custom_config_version": {
                    "type": "string",
                    "description": "Git commit id for Institutional configs.",
                    "default": "master",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "custom_config_base": {
                    "type": "string",
                    "description": "Base directory for Institutional configs.",
                    "default": "https://raw.githubusercontent.com/nf-core/configs/master",
                    "hidden": true,
                    "help_text": "If you're running offline, Nextflow will not be able to fetch the institutional config files from the internet. If you don't need them, then this is not a problem. If you do need them, you should download the files from the repo and tell Nextflow where to find them with this parameter.",
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_name": {
                    "type": "string",
                    "description": "Institutional config name.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_description": {
                    "type": "string",
                    "description": "Institutional config description.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_contact": {
                    "type": "string",
                    "description": "Institutional config contact information.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                },
                "config_profile_url": {
                    "type": "string",
                    "description": "Institutional config URL link.",
                    "hidden": true,
                    "fa_icon": "fas fa-users-cog"
                }
            }
        },
        "r_plots": {
            "title": "r plots",
            "type": "object",
            "default": "",
            "properties": {
                "ctrl_1": {
                    "type": "string",
                    "description": "Generate plots for comparison between controls and samples. Name of control_1. Name must be the same as in the sample sheet. This will be used to generate bar plots for comparison between each sample and the control. This parameter is required for bar plot generation"
                },
                "ctrl_2": {
                    "type": "string",
                    "description": "Name of control_2. Name must be the same as in the sample sheet. This will be used to generate bar plots for comparison between each sample and the control. Can be used together with the 'ctrl_1'-parameter but not alone."
                },
                "make_heatmap": {
                    "type": "boolean",
                    "default": true,
                    "description": "Translate the tax-ids in the likelihood-distribution.tsv from emu to scientific names. A heatmap will be generated from the translated file."
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/$defs/input_output_options"
        },
        {
            "$ref": "#/$defs/generic_options"
        },
        {
            "$ref": "#/$defs/emu"
        },
        {
            "$ref": "#/$defs/short_read_options"
        },
        {
            "$ref": "#/$defs/preprocessing_long_reads"
        },
        {
            "$ref": "#/$defs/krona_options"
        },
        {
            "$ref": "#/$defs/porechop_abi_options"
        },
        {
            "$ref": "#/$defs/phyloseq_options"
        },
        {
            "$ref": "#/$defs/seqtk_options"
        },
        {
            "$ref": "#/$defs/institutional_config_options"
        },
        {
            "$ref": "#/$defs/r_plots"
        }
    ],
    "properties": {
        "fasta": {
            "type": "string",
            "hidden": true
        },
        "master_template": {
            "type": "string",
            "default": "assets/master_template.html",
            "hidden": true
        },
        "igenomes_ignore": {
            "type": "boolean",
            "default": true,
            "hidden": true
        },
        "igenomes_base": {
            "type": "string",
            "hidden": true
        },
        "genome": {
            "type": "string",
            "hidden": true
        },
        "genomes": {
            "type": "string",
            "default": "[]",
            "hidden": true
        },
        "pipelines_testdata_base_path": {
            "type": "string",
            "default": "https://raw.githubusercontent.com/genomic-medicine-sweden/test-datasets/refs/heads/16s/",
            "description": "Base for test data directory",
            "hidden": true
        }
    }
}
